#include <stdio.h>

int main(){
    int length = 10;
    int breadth = 12;
    printf("The area of given rectangle with length %d and breadth %d is: %d\n", length, breadth, length*breadth);
    return 0;
}